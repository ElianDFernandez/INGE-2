services:
  web:
    build: .
    # Comando para levantar el servidor de desarrollo de Django
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"